# Sigma — frontend

React + Vite chat UI for the Phase 3 interface. The built output (`dist/`) is served by
`backend/app.py`, so normal use is just `sigma ui` — run `npm run dev` here only when
working on the UI itself, and `npm run build` to refresh what the backend serves.

See `../README.md` for the whole interface.
